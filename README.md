# mysql-postgres-migrator
A Dockerized tool to migrate MySQL databases to PostgreSQL, including schema, data, indexes, foreign keys, and auto increment conversion with resume support and batch processing.
